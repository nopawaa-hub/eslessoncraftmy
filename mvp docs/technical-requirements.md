Frontend Stack

Required:

React
TailwindCSS
Framer Motion
FullCalendar OR custom calendar engine

Optional:

Zustand
React DnD
TanStack Query
UX Requirements

Must support:

smooth animations
optimistic UI updates
keyboard accessibility
responsive layout
drag-drop interactions
mobile usability
State Management

Global State:

current week
selected lesson
timetable settings
sidebar state

Local State:

form inputs
modal state
temporary drag state
Performance Requirements
lazy load sidebar modules
debounce search
memoized lesson rendering
virtual scrolling if needed
Accessibility

Must include:

keyboard navigation
ARIA labels
readable contrast
responsive text sizing